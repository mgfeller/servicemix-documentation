<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.apache.servicemix</groupId>
    <artifactId>documentation-asciidoc</artifactId>
    <version>6.0.0-SNAPSHOT</version>
    <name>Apache ServiceMix Documentation</name>
    <packaging>pom</packaging>

    <properties>
        <servicemix.version>7.0.0-SNAPSHOT</servicemix.version>
        <activiti.version>5.19.0.2</activiti.version>
        <activemq.version>5.14.3</activemq.version>
        <camel.version>2.16.4</camel.version>
        <cxf.version>3.1.5</cxf.version>
        <karaf.version>4.0.8</karaf.version>
        <asciidoc.outputdirectory>${project.build.directory}/docs</asciidoc.outputdirectory>
        <asciidoctorj.version>1.5.5</asciidoctorj.version>
        <asciidoctor-maven-plugin.version>1.5.5</asciidoctor-maven-plugin.version>
    </properties>

    <prerequisites>
        <maven>3.0.0</maven>
    </prerequisites>

    <build>
        <defaultGoal>process-resources</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctor-maven-plugin</artifactId>
                <version>${asciidoctor-maven-plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>org.asciidoctor</groupId>
                        <artifactId>asciidoctorj</artifactId>
                        <version>${asciidoctorj.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <sourceDirectory>src/main/asciidoc</sourceDirectory>
                    <imagesDir>./images</imagesDir>
                    <doctype>article</doctype>
                    <!-- Attributes common to all output formats -->
                    <attributes>
                        <project-version>${project.version}</project-version>
                        <servicemix-version>${servicemix.version}</servicemix-version>
                    </attributes>
                </configuration>
                <executions>
                    <execution>
                        <id>output-html</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <backend>html5</backend>
                            <sourceHighlighter>coderay</sourceHighlighter>

                            <!--preserveDirectories>true</preserveDirectories-->
                            <attributes>
                                <toc>left@</toc>
                                <toclevels>5@</toclevels>
                                <last-update-label>Generated </last-update-label>
                                <icons>font</icons>
                                <sectanchors>true</sectanchors>
                                <idprefix/>
                                <idseparator>-</idseparator>
                                <docinfo1>true</docinfo1>
                                <nofooter/>
                            </attributes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>